# Extension configuration for the yamllint tool

extends: default

rules:
  document-start: disable
  indentation:
    spaces: consistent
    indent-sequences: whatever
  line-length:
    max: 120
    allow-non-breakable-words: true
    allow-non-breakable-inline-mappings: true
